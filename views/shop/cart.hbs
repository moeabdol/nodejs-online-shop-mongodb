<main>
  {{#if hasProducts}}
    <ul>
      {{#each products}}
        <li>
          {{ this.product.title }} ({{ this.quantity }})
        </li>
      {{/each}}
    </ul>
  {{else}}
    <h1>No products in cart!</h1>
  {{/if}}
</main>
